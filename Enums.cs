using System;
namespace MathGame
{
    internal class Enums
    {
        internal enum Operation
        {
            Add,
            Subtract,
            Multiply,
            Divide,
            Random
        }

        internal enum Difficulty
        {
            Easy,
            Medium,
            Hard
        }

        internal enum Menu
        {
            Play,
            History,
        }
    }
}
